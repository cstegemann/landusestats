start: base gpkg file

-> extract all admin boundaries, "fill in" empty spaces, organize in tree

We cant really be sure "where" the boundary is and we want to process them all, so we load them all into memory first and do our work on them 